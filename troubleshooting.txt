1. /cmd_vel 메시지 타입 불일치 문제

터틀봇 - geometry_msgs/msg/TwistStamped을 받아야 함
Nav2 - geometry_msgs/msg/Twist, geometry_msgs/msg/TwistStamped 두 가지 메세지 타입 동시에 발행

burger.yaml에서 Nav2 관련 노드들의 enable_stamped_cmd_vel을 모두 true로 맞춤

2. Nav2 출력 토픽과 TurtleBot 입력 토픽이 다름

중간에 토픽 목록
/cmd_vel
/cmd_vel_nav
/cmd_vel_smoothed
/cmd_vel_teleop
/tb3/cmd_vel
/tb3/cmd_vel_smoothed

/controller_server → /cmd_vel_nav
/velocity_smoother → /cmd_vel_smoothed 이런 식으로 전역 토픽을 발행함
터틀봇은 /tb3 가 붙은 namespace 토픽을 받아야 함

burger.yaml에서 collision_monitor의 입력/출력을 namespace에 맞게 맞춤
cmd_vel_in_topic: "/cmd_vel_smoothed"
cmd_vel_out_topic: "/tb3/cmd_vel"
enable_stamped_cmd_vel: true

3. /tb3/cmd_vel이 아예 안 나오는 문제, TurtleBot 라이다 /tb3/scan이 안 나오던 문제

/cmd_vel_nav와 /cmd_vel_smoothed에는 값이 나왔는데, 최종 토픽인 /tb3/cmd_vel에는 아무것도 안 나오는 문제 발생 -> controller service, velocity smoother는 정상이고 최종 단계인 collision monitor에서 끊기는 문제 -> /tb3/scan을 구독하지만 터틀봇의 라이다 데이터가 정상적으로 출력되지 않음

LDS-01에서 LDS-02로 라이다 교체 후 ros2 topic echo /tb3/scan sensor_msgs/msg/LaserScan --qos-reliability best_effort --once 확인한 결과 scan 데이터 정상적으로 출력됨.

4. /tb3/scan frame_id가 잘못 붙는 문제

라이다 교체 후 /tb3/scan은 나오기 시작했지만, frame_id가 아래 처럼 출력
frame_id: tb3/tb3/base_scan

robot.launch.py에서 라이다 frame_id를 이미 tb3/base_scan으로 넣어둔 상태였는데, launch 실행 시 namespace도 tb3로 줘서 tb3이 중복으로 써짐.

turtlebot3_bringup/launch/robot.launch.py에서 라이다 frame_id를 다시 아래처럼 수정.
'frame_id': 'base_scan'

수정 후 /tb3/scan 확인 결과: frame_id: tb3/base_scan 정상적으로 출력!!~~

# 최종 요약
1. cmd_vel 타입 불일치
   → enable_stamped_cmd_vel: true로 해결

2. Nav2 출력 토픽과 TurtleBot 입력 토픽 불일치
   → collision_monitor 출력 토픽을 /tb3/cmd_vel로 수정

3. /tb3/cmd_vel이 안 나옴
   → collision_monitor 이후에서 끊기는 문제로 확인

4. collision_monitor가 /tb3/scan을 필요로 하는데 TurtleBot 라이다 scan이 안 나옴
   → 라이다 교체 후 /tb3/scan 정상 출력

5. /tb3/scan frame_id가 tb3/tb3/base_scan으로 잘못 나옴
   → robot.launch.py의 frame_id를 base_scan으로 수정

6. tb3/base_link → tb3/base_scan TF 확인
   → 정상 연결 확인

7. collision_monitor가 /tb3/cmd_vel을 정상 publish하기 시작
   → TurtleBot이 실제 이동 가능해짐

8. 현재는 목적지까지 가지만 주행이 불안정함
   → 이제는 Nav2 파라미터 튜닝 및 map→odom 정렬 단계
   
   토픽 타입 문제
→ namespace 토픽 연결 문제
→ 라이다 scan 미출력 문제
→ scan frame_id 중복 문제
→ collision_monitor 출력 문제
→ 주행 파라미터 튜닝 단계

------------------------------------------------------------------------------------------------------

# 핑키 + 버거 publish point 테스트\
전체적인 계획
1. pinky가 박스를 특정한 ROI 영역 조건을 만족시키면서 인식하는 그 위치 좌표 -> 그 순간까지 만든 map 기준
2. map 기준 목표 좌표로 터틀 자율 주행
3. 근처까지 가서 박스 yolo 인식되는 순간부터 teleop 조작 변경 -> 구현해보니까 시나리오에 좀 부적합 -> 차라리 근처까지 갔을 때 PID 적용해서 정밀제어하는 게 나을 듯

#### 핑키 테스트용
1. pinky -> map_building.launch.xml 수정
rviz2에서 clicked_point를 좌표로 저장하는 노드 추가
이때 rviz의 fixed frame은 반드시 map
2. clicked_point_saver.py 새로 만들기
pinky_navigation/scripts 안에
Publish Point로 클릭한 좌표를 받아서 YAML 파일로 저장하는 노드
/home/user/pinky_box_target.yaml여기에 좌표 저장
-> 다른 터틀은 여기에 저장된 좌표로 가면 될 듯
새로 실행할 때는 이전에 생성된 파일 삭제 후 실행
3. CMakeLists.txt 수정 필요
4. 실행 권한 부여 필요
chmod +x 경로맞게수정/pinky_navigation/scripts/clicked_point_saver.py

### 터틀봇 테스트용
1. navigation2_live_clicked_point.launch.py 새로 생성
/home/user/ROS2_project_ws/turtlebot/src/turtlebot3/turtlebot3_navigation2 경로
2. 같은 경로에 scripts 폴더 만든 후 scripts 안에 clicked_point_to_nav_goal.py 새로 생성
3. 권한 부여
chmod +x /home/user/ROS2_project_ws/turtlebot/src/turtlebot3/turtlebot3_navigation2/scripts/clicked_point_to_nav_goal.py
4. CMakeLists.txt 수정

### 실행
1. pc/pinky/turtle 도메인 id 동일-> 충돌남
	1-0. pinky의 namespace 만들었더니 하드코딩 되어있어서 제대로 안 됨
	1-1.pinky pro 와 turtlebot domain id를 분리 후 domain bridge 사용 -> id가 분리되면서 map과 tf 공유 불가 -> 토픽 모두 받아도 lifecycle inactive
	1-2. domain id 다시 합치고 turtlebot의 namespace 생성
	
2. pinky서버-ros2 launch pinky_bringup bringup_robot.launch.xml
3. pinky서버- ros2 launch pinky_navigation map_building.launch.xml 
4. pc - ros2 launch pinky_navigation map_view.launch.xml
5. 터틀봇- ros2 launch turtlebot3_bringup robot.launch.py namespace:=tb3 multi_robot_name:=tb3
6. pc- ros2 launch turtlebot3_navigation2 navigation2_live_clicked_point.launch.py params_file:=/home/user/ROS2_project_ws/turtlebot/src/turtlebot3/turtlebot3_navigation2/param/burger.yaml

7. /home/user/ROS2_project_ws/turtlebot/src/turtlebot3/turtlebot3_navigation2/param/burger.yaml 수정
8. 터틀봇 bringup 파일 수정
