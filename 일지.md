# 작업 일지 (Work Log)

## 6/9
### 오늘 한 것
1. 좌표 생성되는거 확인함 (clicked_point_saver.py)

### 내일 할 것
1. tb3 (버거) 가 수신한 주행명령 reject (거부???? 왜??)
2. 실제 주행에 필요한 Nav2 서버 (controller_server, planner_server, bt_navigator
3. tf 토픽이 불일치하는거 해결해야함
- /tf, /tf_static = pinky 의 tf, 정상 출력
>- /tb3/tf, /tb3/tf_static 은 publisher 0
>>- 하지만 Nav2 노드들은 /tb3/tf, /tb3/tf_static 구독 --> 에러발생
4. waypoint 주행 테스트 하기

---

## 6/8
### 오늘 한 것

1. **2D pose estimate** — turtle×2 & pinky 잘되는거 확인
   - 2D pose estimate 시 tool properties 에서 initial pose에 각각 네임스페이스 부여
     (e.g. panel → tool properties → 2D pose estimate 꺾쇠 → `/tb3/initial_pose`)
   - 버거·와플은 `/map`, 핑키는 `/pinky/map` 잘 잡히는거 확인
   - rviz 에서 #1 #2 토픽 수정하고 좌측 Display 패널의 map → topic 을 `/map` 에서 `/pinky/map` 으로 수정

2. **pinky 가 SLAM 한 맵 저장**
   - `map_saver.launch.py` 수정 → map_saver Node 에 remapping 라인 추가
     ```python
     remappings=[('map', '/pinky/map')],
     ```
   - 저장 명령:
     ```bash
     ros2 run nav2_map_server map_saver_cli -f <"맵 이름"> --ros-args -r /map:=/pinky/map -p save_map_timeout:=10.0
     ```
     - 실행한 경로에 "맵 이름" 으로 SLAM 한 맵 저장 됨
   - 그 후 더미 맵을 실제 맵과 교체
     - PC 에서 `map_auto_loader.py` 새로 만들고 `navigation2_live_clicked_point.launch.py` 파일에 해당 노드 추가
   - 참고
     - pinky 에서 SLAM 한 맵 저장이 될때도 있고 안될때도 있음 (이유는 몰라) → 임시방편으로 `pinky_map_saver.py` 추가로 만들고 `ros2 run pinky_navigation pinky_map_saver.py` 로 저장
     - 추가 동작 필요해서 안쓰는 쪽으로 최대한 설계할 것

3. **핑키가 움직이면서 publish point로 좌표 찍히는거 확인**
   - YAML 파일에 저장 되는것도 확인, **but** 파일 경로 상대경로로 수정 필요
  

### 내일 할 것
1. ~~컴퓨터 하나에서 다 돌리니까 teleop & 기타 다른 커맨드들 반응 너무 느림 → 터미널 분산 필요~~ --> (6/9 병목은 라즈베리파이, 터미널 나누는건 의미x)
2. 여러개의 좌표를 어느 순서로 어떻게 주행할건지 결정 & 구현 시작

---

## 6/6 – 6/7
### 오늘 한 것

**map 토픽 namespace 꼬인거 해결**

- map 에 관한 topic 여러개 있었는데 dummy map 이랑 pinky 가 SLAM 한 맵이 똑같이 `/map` 으로 발행되고 있었음 → 뭘 봐야할지 몰랐음
- pinky 내부의 `map_building.launch.xml` 에서 맵의 topic 이름을 `/pinky/map` 으로 remapping

**amcl 구독 문제 해결? (임시방편?)**

- 처음엔 map_server 가 `/map` 발행, `/tb3/amcl` & `/wf2/amcl` 이 그 `/map` 을 구독하는게 정상
- `burger`·`waffle.yaml` 파일 내에서도 `map_topic = "/map"` 으로 설정
- but 절대경로 `/map` 이 아니라 상대경로 `map` 으로 받고있었음 (왜??)
- 그래서 상대경로 맵을 구독해서 아무도 발행하지 않는 `/tb3(wf2)/map` 을 구독하고 있었음
  - → odom 제대로 안짜임, frame tree 도 다 끊겨있었음
- 왜 적용이 안됐을까? → launch file 의 이름과 YAML key 가 맞았어야 함??
- 그래서 launch 파일 내에 AMCL node 안에 직접 파라미터를 넣었음:

```python
PushRosNamespace('wf2'),
Node(
    package='nav2_amcl',
    executable='amcl',
    name='amcl',
    # namespace='wf2',
    output='screen',
    parameters=[
        waffle_configured_params,
        {
            'use_sim_time': use_sim_time,
            'map_topic': '/map',
            'global_frame_id': 'map',
            'odom_frame_id': 'wf2/odom',
            'base_frame_id': 'wf2/base_footprint',
            'scan_topic': 'scan',
            'tf_broadcast': True,
        }
    ],
),
```

→ 맞는 접근인지??

### 내일(6/8) 해야하는 거
1. 제일 먼저 확인할 거 = 2D pose estimate 잘되는지 확인 & teleop으로 맵 대충 만들고 저장했을 때 `/pinky/map` 이 잘 잡히는지 확인
2. 핑키가 SLAM 한 맵 저장하는 코드
3. 핑키가 움직이면서 찍은 좌표 저장하는 거 확인
4. 좌표를 설정한 경로의 yaml 파일로 저장하도록 수정 (pinky 안에 `clicked_point_saver.py`)
5. map_server 에 저장된 map topic 을 터틀로 뿌리는 코드 만들기
   - 최초 시작 시 map_server 에는 dummy map, SLAM 한 맵은 `/pinky/map` 으로 만들어짐
   - 그 맵을 dummy map 과 교체
6. `clicked_point_nav_goal` 을 1-b 의 yaml 을 읽어서 좌표를 받고 기동하는 코드로 수정
